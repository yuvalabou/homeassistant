###
# Recorder settings
###
db_url: !secret mariadb_address
purge_keep_days: 30
exclude:
  entities:
    - sensor.time
    - sensor.time_date
    - sensor.date
    - sensor.date_time
  domains:
    - sun
    - weather
