---

purge_keep_days: 30
commit_interval: 10
exclude:
  entities:
    - sensor.time
    - sensor.date
    - sensor.date_time
    - sensor.watchman_last_updated
  entity_globs:
    - sensor.count_*
    - sensor.openweathermap_forecast_*
    - persistant_notification.notification_*
  domains:
    - sun
    - timer
    - weather
    - button
    - media_player
