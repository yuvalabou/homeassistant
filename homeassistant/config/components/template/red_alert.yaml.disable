---

- binary_sensor:
  - name: "RED ALERT"
    unique_id: 719a0391-dc35-44bc-985f-d2ed5d352614
    state: "{{ 'טלמון' in states('sensor.pakar_alerts') }}"
    attributes:
      locations: "{{ states('sensor.pakar_alerts') }}"
    icon: >-
      {% if is_state('binary_sensor.red_alert', 'on') %}
        mdi:alert
      {% else %}
        mdi:alert-outline
      {% endif %}
